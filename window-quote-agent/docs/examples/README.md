# 示例

- **view_agent_graph.ipynb**：在 Jupyter 中打开并运行，可直接可视化报价 Agent 的 LangGraph 图（`display(Image(graph.get_graph().draw_mermaid_png()))`）。  
  请在项目根目录 `window-quote-agent` 下启动 Jupyter，或确保 `sys.path` 包含项目根以便 `from packages.agent.graph import build_quote_graph` 能正常导入。
