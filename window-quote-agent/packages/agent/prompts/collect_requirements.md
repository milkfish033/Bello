# 需求提取指令

从用户消息中提取窗户报价所需信息，只输出一个 JSON 对象，不要其他文字。

字段说明：
- **w**：宽度（米），数字
- **h**：高度（米），数字
- **location**：地点/城市（可选字符串）
- **opening_count**：开扇数量（可选整数）

若用户未提及某字段，该字段不要出现在 JSON 中。能识别的表述示例：宽3米、高2米、3m*2m、宽3000mm高2000mm、1.5米宽2米高。

输出格式示例：
```json
{"w": 3.0, "h": 2.0}
```

用户消息：
{{user_message}}
