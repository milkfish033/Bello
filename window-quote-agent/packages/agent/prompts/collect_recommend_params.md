# 产品推荐参数提取

从用户消息中提取用于产品推荐的信息，只输出一个 JSON 对象，不要其他文字。

字段说明（均为可选字符串，用户未提及则不要出现或为空字符串）：
- **使用场景**：如家用、商用、阳台、厨房
- **特殊需求**：如隔音、防水、抗风、保温
- **价格预算**：如 1 万、5000、预算有限
- **参数**：如尺寸 3m*2m、型材厚度、开扇数量

输出格式示例：
```json
{"使用场景": "家用", "特殊需求": "隔音", "价格预算": "1万左右", "参数": "3米宽2米高"}
```

用户消息：
{{user_message}}
